#!/bin/sh
#
# Garga los archivos de recursos
#

dirresources=/home/lee/.X/xresources

if [ -d $dirresources ]; then
  for resourcefile in $dirresources/*; do
    if [ -f $resourcefile ]; then
        xrdb -merge $resourcefile
    fi
  done
fi
